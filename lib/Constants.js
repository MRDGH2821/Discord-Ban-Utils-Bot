const default_delete_days = 7,
  // eslint-disable-next-line no-magic-numbers
  fourweeks = 4 * 7 * 24 * 60 * 60 * 1000,
  futureIDlen = 19,
  link_expiry_days = 3,
  minIDlen = 17,
  one = 1,
  zero = 0;

module.exports = {
  default_delete_days,
  fourweeks,
  futureIDlen,
  link_expiry_days,
  minIDlen,
  one,
  zero
};
